# Caddy automatically provisions and renews Let's Encrypt TLS certificates.
# This app serves the root domain (both the personal homepage and the shortener).

therushabh.in, www.therushabh.in {
	encode gzip zstd
	reverse_proxy app:3000
}
